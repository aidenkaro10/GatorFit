# Supabase dashboard > Project Settings > API > Project URL
SUPABASE_URL=
# Supabase dashboard > Project Settings > API > Project API keys > "anon public"
SUPABASE_ANON_KEY=
# console.anthropic.com > Settings > API Keys > Create Key (server only, never put this in app/)
ANTHROPIC_API_KEY=
